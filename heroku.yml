build:
  docker:
    web: Dockerfile
    security_opt:
      - seccomp: chrome.json
run:
  web: docker run --security-opt seccomp=chrome.json mercedesgm/capstone
security_opt:
  - seccomp: chrome.json