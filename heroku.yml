# build:
#   docker:
#     web: Dockerfile
#     security_opt:
#       - seccomp: chrome.json
#   security_opt:
#     - seccomp: chrome.json
# run:
#   web: docker run --security-opt seccomp=chrome.json mercedesgm/capstone
# security_opt:
#   - seccomp: chrome.json
version: '3'
build:
  docker:
    web: .
    security_opt:
    - seccomp:"chrome.json"